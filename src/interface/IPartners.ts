export interface IPartners {
    clients: (string | number)[];
    createdAt: string;
    description: string;
    id: string;
    name: string;
    projects: (string | number)[];
    repositoryGit: string;
    urlDoc: string;
}